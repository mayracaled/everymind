import { Outlet } from "react-router-dom";
import { BigCard } from "../../../components/BigCard";
const Perfil = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default Perfil;
